Hello {{ user.full_name or user.username }},

Your DNS Watcher password was changed successfully.

Account: {{ user.username }}
Changed at: {{ user.updated_at.strftime('%Y-%m-%d %H:%M UTC') if user.updated_at else 'just now' }}

If you did not make this change, please contact your administrator immediately.

-- DNS Watcher
